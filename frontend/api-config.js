// API Configuration
// This file will be replaced during Vercel deployment with the actual API URL
// For local development, this will use localhost

// Vercel will inject this via environment variable during build
// For now, we'll use a placeholder that gets replaced
window.API_BASE_URL = window.API_BASE_URL || 'http://localhost:8000/api';


